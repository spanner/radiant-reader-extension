= render :partial => 'controls'

%div.reader_show
  = gravatar(@reader, {:size => 128}, {:class => 'big_gravatar'})
  %h1.reader
    = @reader.name
  - if @reader.description.blank?
    %p No description available.
  - else
    = @reader.description

- content_for :breadcrumbs do
  = link_to 'Home', '/'
  &gt;
  = link_to 'Readers', '/readers'
  &gt;
  = @reader.name

- content_for :title do
  = @reader.name
